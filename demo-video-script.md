# 생각잇기 프롬프트 3분 작품소개 영상 촬영 대본

> 촬영 현장에서는 이 Markdown보다 `demo-video-runbook.html`을 브라우저로 열어 보는 것을 추천한다.  
> 왼쪽은 내레이션, 오른쪽은 클릭 순서로 정리되어 있어 위에서 아래로 따라가기 쉽다.

## 영상 목표

- 길이: 2분 45초~3분 이내
- 형식: 실제 앱 화면 녹화 중심 + 핵심 구조를 짚는 짧은 브릿지 슬라이드 4장
- 메시지: AI가 학생 생각을 대신 쓰는 것이 아니라, 교사는 수업 구조를 통제하고 학생은 자기 생각을 선택·수정·승인하는 Human-in-the-loop 구조로 프롬프트를 완성한다.
- 평가 포인트: 완성도, 교육적 효과, 확장 가능성, 현장 적용성

## 촬영 전 준비

- 교사 계정으로 로그인 가능한 상태를 준비한다.
- 예시 프로젝트 주제는 `교실 속 안전한 생성형 AI 활용`로 준비한다.
- 학생 예시 이름은 실명 대신 `3번 민준`을 사용한다.
- 학생 답변 예시는 미리 메모해 두고 촬영 중 빠르게 입력한다.
- AI 응답 대기 시간, 로그인 시간, 로딩 시간은 편집에서 잘라낸다.
- 화면 녹화 해상도는 16:9, 브라우저 확대는 100% 또는 110%로 맞춘다.
- 슬라이드는 설명용으로 길게 쓰지 않고, 화면 전환과 핵심 메시지 강조용으로만 사용한다.

## 화면에 제시할 슬라이드 구성

> 슬라이드는 전체 영상의 20~25초 안에서만 짧게 사용한다.  
> 핵심은 PPT 발표가 아니라 앱 시연이므로, 슬라이드는 장면을 이해시키는 표지판처럼 쓴다.
> 실제 촬영용 슬라이드는 `demo-video-slides.html` 파일을 브라우저로 열어 사용한다. 좌우 방향키로 넘길 수 있다.

| 슬라이드 | 사용 시간 | 넣을 위치 | 화면 문구 |
|---|---:|---|---|
| 1. 문제 제기 | 0:00~0:08 | 영상 시작 | `AI에게 바로 맡기는 한 줄 프롬프트` / `학생의 생각은 어디에 남을까?` |
| 2. 작품 한 줄 소개 | 0:15~0:22 | 앱 첫 화면 직전 | `생각잇기 프롬프트` / `학생 답변 기반 이미지 생성 프롬프트 수업 도구` |
| 3. HITL 구조 | 0:22~0:35 | 작품 소개 직후 | `Teacher-in-the-loop` / `Student-in-the-loop` / `AI는 보조, 결정은 사람` |
| 4. 교육적 효과 | 2:35~2:45 | 시연 정리 직전 | `과정이 남는 AI 활용 수업` / `교사 통제 + 학생 승인 + AI 보조` |
| 5. 마무리 | 2:55~3:00 | 마지막 화면 | `생각을 잇고, 프롬프트로 완성하다` |

### 슬라이드 디자인 원칙

- 배경은 흰색 또는 아주 연한 회색으로 단순하게 둔다.
- 글자는 한 장에 최대 2줄만 넣는다.
- 강조색은 앱의 초록색 계열과 검정 텍스트를 사용한다.
- 아이콘은 최소화한다. 필요하면 `교사 → AI → 학생` 흐름을 화살표 1개로만 표현한다.
- 슬라이드마다 3~8초만 보여주고 바로 앱 화면으로 넘어간다.

### 슬라이드 3: HITL 구조 추천 화면

```text
Teacher-in-the-loop
수업 목표 · 질문 흐름 · 안전 기준을 교사가 설계

Student-in-the-loop
답변 · 수정 · 최종 승인을 학생이 수행

AI
질문 보조 · 프롬프트 정리 · 안전 점검
```

짧은 화면 문구로는 아래처럼 줄인다.

```text
교사가 설계하고
학생이 결정하며
AI는 보조한다
```

## 전체 타임라인

| 시간 | 장면 | 핵심 메시지 |
|---|---|---|
| 0:00~0:15 | 문제 제기 슬라이드 | 한 줄 프롬프트의 한계 |
| 0:15~0:35 | 작품 소개 슬라이드 + 앱 첫 화면 | 교사와 학생이 함께 들어간 HITL 수업 도구 |
| 0:35~1:10 | 교사 수업 설계 | Teacher-in-the-loop: 교사가 목표와 조건을 통제 |
| 1:10~2:05 | 학생 시연 | Student-in-the-loop: 학생이 선택·수정·승인 |
| 2:05~2:35 | 안전·모니터링 | 교실 운영에 필요한 통제 |
| 2:35~2:55 | 교육적 효과 | 과정이 남고 교과 확장 가능 |
| 2:55~3:00 | 마무리 | 학생 생각을 이어주는 도구 |

## 0:00~0:15 문제 제기

### 화면

슬라이드 1을 8초 정도 보여준 뒤, 앱 화면 또는 생성형 AI 입력 예시 화면 위에 큰 자막을 짧게 띄운다.

### 화면 자막

```text
멋진 그림 그려줘
환경 포스터 만들어줘
```

```text
막연한 한 줄 프롬프트
학생의 생각은 어디에 남을까?
```

### 내레이션

```text
생성형 AI 수업에서 학생들은 막연한 한 줄 프롬프트로 시작하는 경우가 많습니다.
문제는 AI를 쓰는 것이 아니라, 자기 생각 없이 AI에게 맡겨버리는 것입니다.
```

## 0:15~0:35 작품 소개

### 화면

- 슬라이드 2와 슬라이드 3을 짧게 보여준 뒤 앱 첫 화면 또는 교사 로그인 화면으로 넘어간다.
- 상단의 `생각잇기 프롬프트` 서비스명이 보이게 한다.

### 화면 자막

```text
생각잇기 프롬프트
학생 답변 기반 이미지 생성 프롬프트 수업 도구
```

```text
Teacher-in-the-loop
Student-in-the-loop
AI는 보조, 결정은 사람
```

### 내레이션

```text
생각잇기 프롬프트는 학생이 단계별 질문에 답하며 자신의 생각을 구체화하고,
그 답변을 바탕으로 이미지 생성 프롬프트를 완성하는 교육용 웹앱입니다.
이 앱의 핵심은 휴먼 인 더 루프입니다.
교사는 수업 목표와 안전 기준을 정하고, 학생은 자기 생각을 선택하고 수정하고 최종 승인합니다.
AI는 대신 창작하지 않고, 그 과정을 보조합니다.
```

## 0:35~1:10 교사 수업 설계 시연

### 화면 클릭 순서

1. 교사 로그인 화면에서 로그인한다.
2. 프로젝트 화면 또는 수업 설정 화면으로 이동한다.
3. 수업 주제에 `교실 속 안전한 생성형 AI 활용`을 입력하거나 이미 입력된 값을 보여준다.
4. 학습 목표를 보여준다.
5. 필수 요소와 금지/주의 요소를 보여준다.
6. `AI로 수업 설계` 버튼을 클릭한다.
7. 생성된 질문 흐름을 빠르게 스크롤하며 보여준다.
8. `저장하고 모니터링`을 클릭한다.

### 권장 입력값

```text
수업 주제: 교실 속 안전한 생성형 AI 활용
학습 목표: 학생이 생성형 AI를 안전하게 활용하는 장면을 구체화하고, 이미지 생성 프롬프트로 표현한다.
필수 요소: 교실 장면, AI 사용 약속, 학생의 안전한 행동
금지/주의 요소: 개인정보 노출, 혐오 표현, 학생이 말하지 않은 아이디어 임의 추가
```

### 화면 자막

```text
교사가 수업 목표와 조건을 먼저 설정
```

```text
Teacher-in-the-loop
교사가 질문 흐름과 안전 기준을 검토
```

```text
질문 흐름은 AI가 제안, 교사가 검토
```

```text
수업 구조는 교사가 통제
```

### 내레이션

```text
먼저 교사는 수업 주제와 학습 목표를 입력합니다.
필수로 들어가야 할 요소와 금지할 요소도 함께 설정합니다.
AI는 이 수업 맥락에 맞춰 질문 흐름을 제안하고,
교사는 이를 검토한 뒤 학생 활동을 시작합니다.
즉, 교사가 루프 안에서 수업의 방향과 안전 기준을 잡습니다.
```

## 1:10~2:05 학생 화면 시연

### 화면 클릭 순서

1. 모니터링 화면에서 학생 입장 화면을 연다.
2. 학생 이름에 `3번 민준`을 입력한다.
3. 접속 코드를 입력한다.
4. 학생 채팅 화면에 입장한다.
5. 첫 질문에 답변한다.
6. 다음 질문에서 예시 답변 버튼 또는 직접 입력으로 진행한다.
7. 구체적인 장면 답변을 입력한다.
8. 프롬프트 초안 생성 장면을 보여준다.
9. 수정 요청을 입력한다.
10. 최종 프롬프트 승인 장면을 보여준다.

### 학생 답변 예시

```text
AI를 쓸 때 친구의 사진이나 개인정보를 함부로 넣으면 위험하다고 생각해요.
```

```text
교실에서 학생들이 AI 사용 약속을 함께 확인하는 장면을 보여주고 싶어요.
```

```text
칠판에 AI 사용 약속이 보이고, 학생들이 손을 들고 의견을 나누는 모습이면 좋겠어요.
```

```text
칠판에 AI 사용 약속이 더 잘 보이게 해줘.
```

```text
이대로 확정
```

### 화면 자막

```text
단계별 질문으로 생각 구체화
```

```text
Student-in-the-loop
학생이 답하고, 고치고, 최종 승인
```

```text
학생 답변 기반 프롬프트 생성
```

```text
학생이 수정하고 최종 승인
```

### 내레이션

```text
학생은 접속 코드로 들어와 챗봇 질문에 답합니다.
질문은 한 번에 결과물을 요구하지 않고,
주제 이해, 장면 선택, 구체적 묘사 순서로 생각을 넓혀갑니다.
답변이 충분히 쌓이면 앱이 학생의 말을 바탕으로 프롬프트 초안을 만듭니다.
학생은 수정 의견을 내고, 마음에 들 때 최종본으로 승인합니다.
여기서는 학생도 루프 안에 있습니다.
AI 결과를 그대로 받는 것이 아니라, 자신의 언어로 답하고 수정하며 최종 결정을 합니다.
```

## 2:05~2:35 안전 기능·교사 모니터링

### 화면 클릭 순서

1. 학생 입력창에서 붙여넣기를 시도한다.
2. 붙여넣기 차단 또는 안전 안내 메시지를 보여준다.
3. 교사 모니터링 화면으로 돌아간다.
4. 학생 목록에서 현재 단계, 경고 수, AI 사용량을 보여준다.
5. `3번 민준` 학생 상세 화면을 연다.
6. 대화 기록, 최종 프롬프트, AI 분석 영역을 보여준다.

### 화면 자막

```text
붙여넣기 차단
```

```text
부적절 표현·주제 이탈 점검
```

```text
교사 실시간 모니터링
```

```text
Teacher-in-the-loop
필요한 순간 교사가 개입
```

### 내레이션

```text
교실 수업에서 중요한 것은 안전한 운영입니다.
이 앱은 붙여넣기 시도를 막고,
부적절한 표현이나 주제 이탈 답변을 점검합니다.
교사는 모니터링 화면에서 학생별 진행 단계, 경고 기록,
최종 프롬프트, AI 분석을 확인하며 필요한 순간 개입할 수 있습니다.
그래서 AI 활용 과정이 교실 밖으로 벗어나지 않고, 교사의 수업 운영 안에 머뭅니다.
```

## 2:35~2:55 교육적 효과·확장 가능성

### 화면

- 교사 모니터링 화면 전체
- 학생 상세 화면의 최종 프롬프트
- 가능하면 프로젝트 목록 화면
- 앞부분에 슬라이드 4를 5초 이내로 넣고 바로 실제 모니터링 화면으로 돌아온다.

### 화면 자막

```text
과정이 남는 AI 활용 수업
```

```text
교사와 학생이 함께 통제하는 AI 수업
```

```text
여러 교과·주제로 확장 가능
```

### 내레이션

```text
생각잇기 프롬프트는 생성형 AI 사용법을 가르치는 데서 끝나지 않습니다.
학생이 자기 생각을 말하고, 다듬고, 검토하는 과정을 남깁니다.
교사는 수업의 기준을 만들고, 학생은 결과물을 직접 승인합니다.
이 Teacher-in-the-loop와 Student-in-the-loop 구조 덕분에 AI 활용이 평가 가능한 학습 과정이 됩니다.
주제만 바꾸면 안전, 환경, 역사, 미술, 독서 활동 등 다양한 수업으로 확장할 수 있습니다.
```

## 2:55~3:00 마무리

### 화면

- 완성된 최종 프롬프트 화면
- 또는 앱 첫 화면과 푸터가 보이는 화면
- 마지막 3~5초는 슬라이드 5 또는 앱 화면 위 큰 자막으로 마무리한다.

### 화면 자막

```text
생각을 잇고, 프롬프트로 완성하다
```

### 내레이션

```text
생각잇기 프롬프트는 AI가 답을 대신 만드는 도구가 아니라,
교사와 학생이 함께 루프 안에서 AI를 통제하며,
학생의 생각을 이어주는 수업 도구입니다.
```

## 편집 체크리스트

- 전체 길이는 2분 45초 안팎으로 맞춘다.
- 로그인, 로딩, AI 대기 시간은 과감하게 자른다.
- 교사 화면, 학생 화면, 모니터링 화면을 모두 넣는다.
- 슬라이드는 최대 5장만 사용하고, 총 노출 시간은 25초를 넘기지 않는다.
- 학생 실명은 사용하지 않는다.
- 최종 프롬프트가 생성되는 장면은 반드시 크게 보여준다.
- 교사 설정 장면에서는 `Teacher-in-the-loop`, 학생 승인 장면에서는 `Student-in-the-loop` 자막을 넣는다.
- 안전 기능은 길게 설명하지 말고 실제 경고 화면으로 보여준다.
- 마지막 5초에는 서비스명과 핵심 문장이 남게 한다.

## 심사위원 강조 문장

```text
AI가 대신 쓰지 않고, 학생 답변을 바탕으로 프롬프트를 만듭니다.
```

```text
Teacher-in-the-loop: 교사가 수업 목표와 금지 요소를 먼저 설정합니다.
```

```text
Student-in-the-loop: 학생은 답하고 수정하고 최종 승인합니다.
```

```text
교사는 진행 상황과 안전 경고를 실시간으로 확인합니다.
```

```text
주제만 바꾸면 여러 교과 수업으로 확장할 수 있습니다.
```

## 촬영용 한 문장 요약

```text
생각잇기 프롬프트는 교사가 설계하고, 학생이 답하고, AI가 보조하며, 최종 결정은 다시 학생과 교사에게 남기는 수업용 프롬프트 빌더입니다.
```
